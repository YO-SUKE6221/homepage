baseurl: https://example.com
languageCode: en-us
theme: hugo-theme-stack
paginate: 3
title: Example Site
copyright: Example Person

build:
  sass:
    # LibSass Options
    # for extended Sass/SCSS processing
    # if you want to use dart-sass, delete this.
    # Hugo uses the extended version by default, so you can just delete this block
    # extended = false
